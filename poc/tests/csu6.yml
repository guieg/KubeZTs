apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota-default
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "30"
    services: "5"
    persistentvolumeclaims: "10"